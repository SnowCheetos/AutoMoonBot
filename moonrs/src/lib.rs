pub mod data;
pub mod edges;
pub mod graph;
pub mod nodes;
pub mod utils;

extern crate nalgebra as na;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
